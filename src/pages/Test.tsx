import { FC } from "react";
import Summary from "@/components/Summary";

type TestProps = {};

const Test: FC<TestProps> = ({}) => {
  return (
    <div>
      <Summary />
    </div>
  );
};

export default Test;
